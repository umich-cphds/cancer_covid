# libraries -----------
ally::libri(tidyverse, data.table, cli, janitor, logistf, broom, broomExtra,
            glue, ggstance, cowplot, openxlsx, table1, RColorBrewer)

# suppressPackageStartupMessages({
#   library(tidyverse)
#   library(data.table)
#   library(cli)
#   library(janitor)
#   library(logistf)
#   library(broom)
#   library(broomExtra)
#   library(glue)
#   library(ggstance)
#   library(cowplot)
# })
